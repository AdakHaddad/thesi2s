# Vivado pointers

This file has been minimized to avoid stale documentation being picked up by tools.

For the authoritative Vivado block design and reconstruction scripts, see [PROJECT_REFERENCES.md](PROJECT_REFERENCES.md) and the `nobufgmux/` directory.

If you need to reconstruct a local Vivado project, run the reconstruction script from the Vivado Tcl console inside the `nobufgmux/` project directory per that project's README.
